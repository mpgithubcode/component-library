import BarChart from "./BarChart"

export default {
  title: "Components/BarChart",
  component: BarChart,
  args: {
  },
  argTypes: { 
  },
}

const Template = args => <BarChart {...args} />

export const Bar_Chart = Template.bind({})
Bar_Chart.args = {
}
